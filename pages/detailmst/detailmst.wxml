<view wx:if="{{mst}}">
  <image class="cover" src="{{mst.image}}" mode="aspectFill"></image>
  <view class="body">
    <text class="name">{{mst.name}}</text>
    <text class="title">{{mst.title}}</text>
    <text class="para">{{mst.intro}}</text>
  </view>
</view>
